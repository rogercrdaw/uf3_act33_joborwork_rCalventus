<nav class="navbar navbar-expand-lg navbar-light alert-info">
        <a class="navbar-brand text-info" href="{{ path('home') }}">JorOrWork</a>
        {# <a class="navbar-brand text-primary" href="#">JorOrWork</a> #}

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <!--MENU ESQUERRA-->
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('ofertas') }}">Ofertas
                {# <span class="sr-only">(current)</span> #}
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('empresas') }}">Empresas</a>

            </li>
        </ul>
    <!--MENU DRETA-->
    
    {% if app.user %}
        <ul class="nav justify-content-end">
            <li class="nav-item">
               <span class="nav-link">
                    {% if app.user.roles[0] == "ROLE_USER" %}
                    Usuari
                    {% elseif app.user.roles[0] == "ROLE_EMPRESA" %}
                    Empresa
                    {% elseif app.user.roles[0] == "ROLE_ADMIN" %}
                    Admin
                    {% endif %}
               {{ app.user.username }}</span>
            </li>
            <li>
                <a class="nav-link text-success" href="{{ path('user_update') }}" alt="Editar perfil" title="Editar perfil"><i class="fa fa-user"></i></a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-danger" href="{{ path('app_logout') }}" alt="Salir" title="Salir"><i class="fa fa-sign-out"></i></a>
            </li>
        </ul>
    {% else %}
        <ul class="nav justify-content-end nav-pills">
            <li class="nav-item">
                <a class="btn btn-outline-info" style="margin-right:5px;" href="{{ path('app_login') }}">LOGIN</a>
            </li>
            <li class="nav-item">
                <a class="btn btn-info" href="{{ path('app_register') }}">SIGN IN</a>
            </li>
        </ul>
    {% endif %}

    <!--FORM LOGIN-->
        {# <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form> #}
    </div>
</nav>
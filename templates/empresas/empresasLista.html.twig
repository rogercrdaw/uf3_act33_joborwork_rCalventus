{% extends 'base.html.twig' %}

{% block body %}

	<h3>{{ page_name }}</h3>

	<table class="table table-hover">
		<tr class="alert-secondary">
			<td>Empresa</td>
			<td>Tipus</td>
			<td>Ofertes</td>
		</tr>
		{% for empresa in empreses_llista %}
			<tr>
				<td><a href="{{ path('empresa_detall', {id:empresa.id}) }}" class="card-link text-info">{{empresa.nom}}</a></td>
				<td>{{empresa.tipus}}</td>
				<td>??</td>
			</tr>
		{% endfor %}
	</table>
{% endblock %}
